services:
  health-signal:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    volumes:
      - .env:/app/.env
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
